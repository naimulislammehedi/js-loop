// used to iterate through arrays, strings, and other iterable objects 

// array example
const fruits = ["apple", "banana", "orange"]; 

for (const fruit of fruits) {
    console.log(fruit); 
}

// string example 
const name = "mehedi"; 
for (const letter of name) {
    console.log(letter); 
}